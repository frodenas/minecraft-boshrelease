set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Copy Minecraft package
echo "Copying Minecraft package..."
cp -a ${BOSH_COMPILE_TARGET}/minecraft/minecraft_server.*.jar ${BOSH_INSTALL_TARGET}
